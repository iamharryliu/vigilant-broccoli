# CLAUDE — infrastructure/local

`## Networking` and `## Observability` in [README.md](./README.md) are manually curated per [docs/app-readme-pattern.md](../../docs/app-readme-pattern.md) — `/update-readmes` must never delete or invent entries in either section, only fix an entry the user confirms is stale.
