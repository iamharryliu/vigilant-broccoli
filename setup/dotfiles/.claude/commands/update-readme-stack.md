Update or create the app's README.md with a "## Stack" section: a minimal markdown bullet list of the technologies the application is built on (framework, language, database, auth, hosting, key libraries/services).

Keep it minimal — just the bullet list, no prose. If a "## Stack" section already exists, update it in place rather than duplicating.

Derive the stack from the actual code and dependencies (package.json, imports, env vars, config) so newly introduced tech is included — e.g. if MongoDB was just added, it should now appear in the list. Double check the list against the code and fix anything that is missing or incorrect.
