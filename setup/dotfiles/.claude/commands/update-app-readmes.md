Review and refresh the `README.md` of every app under `projects/nx-workspace/apps/*` to match `docs/app-readme-pattern.md`.

For each app, derive its current purpose and stack from the actual code and dependencies (`package.json`, imports, env vars, config), then create or update the README in place. Double check each README against the code and fix anything missing or incorrect.
