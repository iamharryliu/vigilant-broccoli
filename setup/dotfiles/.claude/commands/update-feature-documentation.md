Update or create a docs/features/this-feature.md to reflect changes in this conversation. I do not want verbose specs. Write as minimal as possible. Preferably just a few headers and markdown lists.

Then double check the doc against the actual code and fix anything that is incorrect (file paths, names, env vars, endpoints, defaults, behavior).
