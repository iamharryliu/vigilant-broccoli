Review the current changes against this repo's cleanup checklist, apply the cleanups, and report any critical issues or recommendations called out there.

Look for the checklist at `docs/refactor-code-cleanup.md` (or a similarly named doc referenced from `CLAUDE.md`). If none exists, apply a general cleanup pass instead: remove dead code, unused imports/vars, leftover debug statements, and stale comments; make naming and formatting consistent with surrounding code; flag (don't silently fix) anything that looks like a real bug.
