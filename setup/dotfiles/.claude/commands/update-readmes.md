Review and refresh every `README.md` in the repo to match this repo's README pattern.

- If a pattern doc exists (e.g. `docs/app-readme-pattern.md` or similarly named), follow it — apply the app-specific pattern to app directories and the infrastructure-specific pattern to infrastructure directories, if the repo distinguishes them.
- If no pattern doc exists, infer a consistent pattern from the most complete/representative existing READMEs in the repo and confirm it with the user before applying it broadly.

Double check each README against the code and fix anything missing or incorrect.
