# Repo Audit — Outstanding Issues

Backlog grouped by category. Each section is a markdown table (`ID | Priority | Description | Recommended Fix`), ordered by priority. Priority values: **P1** (highest) → **P2** → **P3**, then **NA** (nice-to-have or deliberately-accepted risk). IDs are stable 6-hex handles; `pnpm agentic:task:solve <id>` resolves an item from its row.

## Table of Contents

- [Security](#security)
- [Performance](#performance)
- [Maintenance](#maintenance)
- [Feature Enhancements](#feature-enhancements)
- [UI Cleanup](#ui-cleanup)
- [Not so serious](#not-so-serious)

## Security

| ID     | Priority | Description                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Recommended Fix                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| ------ | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 17daeb | P2       | GCP VMs on `default` network; default-allow-ssh likely open on the Vault VM (`infrastructure/terraform/main.tf:96-127`). Only WireGuard-UDP and IAP-SSH rules are defined; nothing deletes GCP's auto-created `default-allow-ssh` (tcp/22 from 0.0.0.0/0), so port 22 on the Vault VM is probably internet-open, defeating the IAP-only design.                                                                                                                                                                                                                                                   | Dedicated VPC with explicit rules only, or delete/manage `default-allow-ssh` in Terraform so the repo is the source of truth.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 1a2b1b | P2       | Default compute SA has project-wide secret access + `cloud-platform` scope (`infrastructure/terraform/main.tf:103-104` + project-level secretAccessor grants). Every VM (including transient Packer build VMs) using the default SA can read all project secrets and use the Vault KMS unseal key — any RCE yields Vault root token + WG private key + Bitwarden password.                                                                                                                                                                                                                        | Dedicated SA for `vb-free-vm` with per-secret grants (needs only `VB_VM_WG_*`, `VB_VM_CLOUDFLARED_TUNNEL_TOKEN`) + the KMS key; run Packer with a separate minimal SA.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 20177f | P2       | RabbitMQ management UI + SSH exposed to 0.0.0.0/0; user is `admin` (`infrastructure/terraform/oci.tf:49-90`). The management UI (TLS but single-factor, username `admin`) is internet-reachable — continuous credential-stuffing surface. SSH/22 is world-open on all three OCI VMs.                                                                                                                                                                                                                                                                                                              | Restrict 15671 and 22 to home/WG egress IPs. (5671 AMQPS must stay open for fly.io consumers — justified.)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 21290b | P2       | Vault root token used as everyday credential + passed on command lines (`infrastructure/terraform/main.tf` `VB_VM_VAULT_ROOT_TOKEN`; `packer/scripts/run-vault-*.sh`, `rotate-*.sh`, `scripts/post-apply.sh:119-122`). The never-expiring root token is the routine operational credential and is interpolated into `gcloud compute ssh --command="… VAULT_TOKEN='…' …"` strings (visible in `ps` on the VM); unseal keys likewise passed as CLI args.                                                                                                                                            | Scoped admin policy + short-TTL token role for scripts; revoke the root token (keep recovery keys); pass secrets via stdin — `sync-socket-server-token.sh` already does the stdin pattern correctly, copy it.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 229043 | P2       | No rate limiting on LLM/chat endpoints — cost abuse (`projects/nx-workspace/apps/api/llm-service/src/routes/llm.ts`, `chat.ts`). PR #115 clamped `numOutputs` (`MAX_NUM_OUTPUTS`) but no `@fastify/rate-limit` anywhere (vb-express sets `rateLimit: { enabled: false }`) and no input token-size cap — a caller with the shared token can fire unlimited paid model calls.                                                                                                                                                                                                                       | Add per-key/IP rate limiting on LLM + chat routes and cap input size.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 240ff8 | P2       | Open email relay to arbitrary recipients from a trusted domain (`projects/nx-workspace/apps/api/email-service/src/main.ts:135-171`). `/api/send-email` and `/api/queue-emails` validate only that `to`/`subject` exist; `to`, `html`/`text`, and `from` are fully caller-controlled and sent via Resend from verified `harryliu.dev`. Whoever holds `SHARED_APP_TOKEN` can send unlimited arbitrary mail from a trusted domain — a spam/phishing relay that risks domain reputation.                                                                                                              | Allowlist `to`/`from`, cap batch size, add rate limiting, treat the send token as more sensitive.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 27b216 | P2       | Open room subscription on socket server — message eavesdropping (`projects/nx-workspace/apps/socket-server-socketio/src/main.ts` `handleSubscribe`; `cors: { origin: '*' }`). Publishing is gated by `SENDER_TOKEN`, but subscribing is completely open: any anonymous client can `subscribe` to any `roomFor(app, receiverId)` with no ownership check — guess/know a `receiverId` and receive every message to it.                                                                                                                                                                              | Authenticate receivers and authorize ownership of `receiverId` before `socket.join`. Related hardening tracked in 9cbd33.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 282218 | P2       | SMS send relay with caller-controlled from/to/body — toll fraud (`projects/nx-workspace/apps/api/vb-express/src/routes/messaging.ts` `/send-text-message`). `body`/`from`/`to` pass straight to `Twilio.messages.create` with no allowlist and no rate limit — a leaked messaging key means SMS to arbitrary premium/international numbers on the owner's account.                                                                                                                                                                                                                                | Restrict `from` to owned numbers, allowlist/validate `to`, rate-limit.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 306cc4 | P2       | Single shared Vault role gives every workflow the whole secret store (`infrastructure/terraform/packer/scripts/run-vault-post-init.sh` policy grants `kv/data/secrets/*`; consumed by `.github/actions/vault-secrets/action.yml`). Role `github-actions-role` is usable by any job with `id-token: write`; the `secrets:` filter is cosmetic — a compromised step can mint its own OIDC token and dump the whole KV path. Partially addressed for `ci-pr-check.yml` (own `github-actions-pr-check-role` scoped to `kv/data/ci-pr-check`); `deploy.yml`/`notify-start` still share the broad role. | Per-purpose Vault roles bound with `bound_claims.job_workflow_ref` (the rotate role in the same script shows the pattern), exposing only the keys each workflow needs.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 331697 | P2       | SSH host-key verification effectively disabled when pushing `SHARED_APP_TOKEN` (`infrastructure/terraform/packer/scripts/sync-socket-server-token.sh:61-63` via `ci-rotate-secrets.yml`). `StrictHostKeyChecking=accept-new` plus `ssh-keygen -R` before connecting = trust-on-first-use every run, then the rotated token is piped over that connection; a DNS hijack/MITM of `socket.harryliu.dev` is accepted and receives the new token. Same TOFU in `cron-backup.yml:79,85` (lower impact).                                                                                                 | Store the VMs' host public keys (not secret) in repo/Vault, write to `known_hosts` with `StrictHostKeyChecking=yes`, drop the `ssh-keygen -R`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| 9cbd33 | P2       | Socket-server hardening. Short-lived JWTs instead of the long-lived `SHARED_APP_TOKEN` (`route.ts` signs `{ exp: +60s }` with a Vault `JWT_SECRET`, server middleware verifies; optional per-room scope). App-level: CORS allowlist via `ALLOWED_ORIGINS` env (currently `*`), payload size cap, last-message cache per room on subscribe.                                                                                                                                                                                                                                                        | Implement the JWT + CORS-allowlist + payload-cap items above. Subscribe-side authorization (any anonymous client can join any room) is tracked separately in 27b216.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| a5fb01 | P2       | upptime GitHub App private key has no rotation automation. The upptime crons (`.github/workflows/cron-upptime.yml`, `cron-upptime-response-time.yml`) mint per-run installation tokens from a dedicated GitHub App (App ID `4350545`; Contents + Issues RW) via `infrastructure/agent-sandbox/mint-github-app-token.sh` using `UPPTIME_GH_APP_PRIVATE_KEY` from Vault `kv/data/secrets`. This key lets a push bypass the `main` ruleset (`github.tf` bypass actor), yet nothing rotates it — absent from `ci-rotate-secrets.yml` and the manual inventory in `secret-management.md`.              | 1. Add `UPPTIME_GH_APP_PRIVATE_KEY` to the manual rotation inventory in `docs/infrastructure/secret-management.md` (generate new key at github.com/settings/apps, `base64 -i key.pem \| tr -d '\n'`, `vault kv patch kv/secrets UPPTIME_GH_APP_PRIVATE_KEY=...`, delete old key; no redeploy — crons read Vault fresh).<br>2. Optional (preferred): script `pnpm secret-rotation:upptime-app-key` modeled on `rotate-profile-deploy-key.sh` — note GitHub has no API to generate an app key, so a rotator can only verify a human-supplied key and revoke old ones.<br>3. If scripted, wire into `pnpm secret-rotation:all` and the table in `docs/infrastructure/secret-rotation-implementation.md`.<br>4. Cross-check `secret-management.md`'s Deploy-secrets inventory actually lists the key. |
| c4a917 | P3       | nx-cache Worker PUT has no size cap (`infrastructure/cloudflare-workers/nx-cache/index.js`, PUT branch). The immutable-write race is fixed (conditional `put(..., { onlyIf: { etagDoesNotMatch: '*' } })`), but PUT still streams the body to R2 with no size limit — a leaked write token (held only by `deploy.yml`) can run up R2 storage until the `nx_cache_r2_ttl_seconds` lifecycle rule expires it.                                                                                                                                                                                       | Reject PUT when `Content-Length` exceeds a reasonable cache-artifact size (e.g. a few hundred MB).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| a10595 | P3       | RabbitMQ TLS server-identity verification disabled (`apps/api/email-service/src/main.ts:30`, `email-subscription-service/src/main.ts:49`, `checkServerIdentity: () => undefined`). CA is still pinned.                                                                                                                                                                                                                                                                                                                                                                                            | Drop the override or pin the expected CN/SAN.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ae83d3 | P3       | Mobile app stores Google `provider_token` in `localStorage` — XSS-exfiltratable (`apps/vb-manager-next-mobile/src/app/providers/auth-provider.tsx:63`).                                                                                                                                                                                                                                                                                                                                                                                                                                           | Prefer sessionStorage/in-memory.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| aefbbb | P3       | RabbitMQ TLS private key world-readable (`infrastructure/terraform/cloud-init-rabbitmq.yaml:23-24`, `server.key` at `0644`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Set `0600` (gitea/code-server cloud-inits already do).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| b33395 | P3       | Plaintext RabbitMQ ports published on host (`infrastructure/terraform/cloud-init-rabbitmq.yaml:59-62`, 5672/15672 on 0.0.0.0; Docker bypasses host iptables).                                                                                                                                                                                                                                                                                                                                                                                                                                     | Remove the mappings or bind `127.0.0.1:`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| b5b994 | P3       | Committed cert leaks VM public IP (`projects/nx-workspace/scripts/vault-ca.crt`, SAN `IP Address:136.116.117.204`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Gitignore the generated cert / distribute out-of-band.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| b92f95 | P3       | code-server login password == sudo password (`infrastructure/terraform/cloud-init-code-server.yaml:80-81`; leaked web password → container root).                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Separate the two.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| bb6d65 | P3       | Unverified binary / `curl\|bash` installs: `cron-backup.yml:125` (mongodb-tools `.deb`, no checksum), `deploy-github-profile.yml:39` + `cron-backup.yml` (`ssh-keyscan` TOFU), `cloud-init-code-server.yaml` (nodesource `curl\|bash` as root each VM init), Packer `provision.sh:29-30` (`get.docker.com`).                                                                                                                                                                                                                                                                                      | Pin/verify checksums; hardcode GitHub's published host keys.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |

## Performance

| ID     | Priority | Description                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Recommended Fix                                                                                                                                                                                                                                                                        |
| ------ | -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 16dbe5 | P1       | code-server VM re-provisions its entire toolchain (~1GB+) on every container (re)start (`infrastructure/terraform/cloud-init-code-server.yaml:27-66`, `:72` `:latest`). The `/custom-cont-init.d` script runs at every start: `apt-get update/install` unconditionally, and `command -v`-guarded blocks (Node, ~700MB `google-cloud-cli`, claude-code) survive only while the same image filesystem is alive; it also clones/pulls this repo and runs `setup/linux/apt-packages.txt` + `install.sh` in full each start. Watchtower no longer auto-updates the image, but any deliberate redeploy (`terraform apply` recreation or manual `docker compose pull && up -d`) discards the toolchain layer: 1GB+ of downloads and minutes of apt/npm before code-server starts. | Bake a derived image (`FROM lscr.io/linuxserver/code-server` + toolchain in a Dockerfile), push to Docker Hub, reference it here. Cheaper fallback: install the toolchain under the persistent `/config` volume (or gate the script on a `/config` marker file) and pin the image tag. |
| 47b3fb | P2       | Queue consumers live inside auto-stopped fly machines — email delivery stalls while stopped. Both email services start their AMQP consumer in the web process but run with `auto_stop_machines = 'stop'`, `min_machines_running = 0` (`deployment-configs/fly-configs/{production,staging}-email-service.toml` and email-subscription tomls). Outbound AMQP doesn't keep fly machines alive: when stopped, queued emails sit until an HTTP request (in practice the Upptime ping) wakes the machine — minutes of delay plus RabbitMQ reconnect churn per cycle.                                                                                                                                                                                                            | `min_machines_running = 1` for email-service, or move consumers to the always-on VM, or accept and document the delay.                                                                                                                                                                 |
| 496949 | P2       | Upptime pings + auto-stop = machines cycle start/stop near-continuously (`.upptimerc.yml` `sites:` includes all fly apps). Each check wakes a stopped machine; fly's idle sweep stops it minutes later. A large fraction of always-on runtime is paid anyway, real users still hit cold starts (vb-express runs better-auth migrations at boot), and every cycle re-handshakes RabbitMQ/Supabase.                                                                                                                                                                                                                                                                                                                                                                          | Pick one per service: `min_machines_running = 1` for user-facing vb-express, or `auto_stop_machines = 'suspend'` (resume sub-second vs full boot), or lengthen the Upptime interval.                                                                                                   |
| 5b34e7 | P2       | `googleapis` meta-package import — heavy cold-start cost in vb-express (`apps/api/vb-express/src/routes/tasks.ts:2`, `import { google } from 'googleapis'`). Indexes every Google API at require time on a shared-1-cpu machine that cold-boots constantly (496949), and drags a huge tree into the pruned image.                                                                                                                                                                                                                                                                                                                                                                                                                                                          | `@googleapis/tasks` only, or plain `fetch` against the REST API (the code already does this for task lists).                                                                                                                                                                           |
| 69d76a | P2       | Whiteboard broadcasts the full document on every keystroke (`libs/@vigilant-broccoli/react-lib/src/whiteboard/useWhiteboardRoom.ts:161-172`). `setContent` sends the entire content string per `onChange`, no debounce; typing at 60 WPM in a large doc ≈ 5 full-payload Supabase realtime messages/sec per participant, every peer re-rendering per message.                                                                                                                                                                                                                                                                                                                                                                                                              | Throttle/debounce sends (~200ms trailing) while updating local state immediately; optionally send diffs.                                                                                                                                                                               |
| 6b3c12 | P2       | No route-level code splitting in any SPA. Zero `lazy(` hits across `apps/ui`, `apps/findme`, `apps/whiteboard`; `cloud-8-skate-angular` eagerly imports every page in `routes.const.ts` (no `loadComponent`); `personal-website-react/src/app/app.tsx` statically imports all 9 pages, so `marked` ships in the initial chunk for home-page visitors.                                                                                                                                                                                                                                                                                                                                                                                                                      | `React.lazy` + `Suspense` for heavy routes; `loadComponent` for Angular.                                                                                                                                                                                                               |
| 6fc308 | P2       | vb-manager root layout is `'use client'` with a hidden always-mounted dialog tree (`apps/ui/vb-manager-next/src/app/layout.tsx:1` + `(pages)/layout.tsx` mounting `FloatingIslandComponent` inside `display: none` on every page). Includes the chatbot dialog which statically imports `react-markdown`, plus email/calendar/weather/pomodoro dialogs, all in the shared layout bundle executing on first paint.                                                                                                                                                                                                                                                                                                                                                          | `next/dynamic` for each dialog (needed only after a shortcut/click); move `'use client'` down from the root layout so pages can opt into server rendering later.                                                                                                                       |
| 7e1071 | P2       | hearth ships zero optimized images; 1920px originals rendered as 96px thumbnails. No `next/image` anywhere in hearth; R2 originals stored up to 1920px/q85 (`api/where-is/image-processor.ts`) render via plain `<img>` at `h-24 w-24` in lists — a 10-photo area downloads several MB just for thumbnails.                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Write a second ~256px `-thumb.jpg` variant at upload (sharp is already in the pipeline) and use it in lists; or route R2 URLs through `next/image` `remotePatterns`.                                                                                                                   |
| 8290e3 | P2       | speed-test route: no in-flight dedupe — concurrent polls stack `speedtest` processes (`apps/ui/vb-manager-next/src/app/api/speed-test/route.ts:19-32`). The 5-min cache timestamp is written only after the run completes; the client polls every 30s and a run takes 20–40s, so once the cache expires every poll/tab arriving during the run launches another `speedtest`, each saturating the uplink the others are measuring.                                                                                                                                                                                                                                                                                                                                          | Store the in-flight promise in module scope and await it for concurrent callers; decouple the client interval from the server cache TTL.                                                                                                                                               |
| 83f236 | P2       | Docker daemon baked into and enabled on the 1GB e2-micro; nothing on that VM uses it (`infrastructure/terraform/packer/scripts/provision.sh:29-32`). Docker installed and `systemctl enable`d on the GCP free-tier VM, which runs Vault as a native binary plus WireGuard/cloudflared; nothing runs a container. Idle dockerd+containerd ≈ 70–100MB RSS — ~10% of total RAM.                                                                                                                                                                                                                                                                                                                                                                                               | Drop the Docker install from `provision.sh` (or at least don't enable it).                                                                                                                                                                                                             |
| 8bc0e1 | P2       | Backup bucket versioning silently multiplies retention ~13x past the intended 7 copies (`infrastructure/terraform/main.tf:439-443`). The bucket has `versioning { enabled = true }` with a 90-day age Delete rule, while `cron-backup.yml` prunes to 7 backups via `gsutil rm` — which with versioning only makes objects noncurrent. Net: ~90 daily full dumps (repo + Gitea + MongoDB + Supabase) billed, not 7.                                                                                                                                                                                                                                                                                                                                                         | Disable versioning (dated filenames already provide history), or add a `days_since_noncurrent_time` lifecycle rule (e.g. 7).                                                                                                                                                           |
| 9a3554 | P2       | Every `pnpm tf:*` command pays a ~5–10s Bitwarden/gcloud secrets bootstrap (`infrastructure/terraform/scripts/load-vault-tf-env.sh:19-35`). Each invocation runs `bw status`, a network `gcloud secrets versions access`, `bw unlock`, `bw list folders`, `bw list items` — each `bw` call a ~0.5–1s Node CLI startup plus vault decryption, on every `tf:plan`/`tf:apply`/`tf:import`.                                                                                                                                                                                                                                                                                                                                                                                    | Cache `BW_SESSION` (0600 file under `$XDG_RUNTIME_DIR` or keychain) and the resolved token exports with a short TTL; re-derive only on failure.                                                                                                                                        |
| 9c6df0 | P2       | Local `nx serve` of fly services fetches Vault token + secrets over the network on every start (`apps/api/llm-service/project.json` `serve` → `scripts/fetch-secrets.ts` → `scripts/gcp-vault-token.ts`). `gcloud secrets versions access` + a Vault read over WireGuard on every dev-server start.                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Cache fetched env with a TTL (`--refresh` to force); the code already short-circuits on `VAULT_TOKEN` — document/wrap that path.                                                                                                                                                       |
| 5bdea5 | P2       | Vite/Angular bundle-size warnings. `component-library`: JS 676.91 kB / CSS 725.17 kB — CSS from `src/main.tsx` importing the full `@radix-ui/themes/styles.css`; JS from `react-sandbox/src/lib/ComponentSandbox.tsx` statically importing all 13 demos + 6 utilities at module top-level though only one is open by default. `docs-md`: JS 520.30 kB / CSS 733.63 kB — same full Radix CSS import used only for the `<Theme>` wrapper. `journal`: JS 519.53 kB / CSS 733.63 kB — same.                                                                                                                                                                                                                                                                                    | `React.lazy` + `Suspense` per demo in ComponentSandbox; drop Radix Themes in docs-md/journal for a minimal custom reset (or Radix's modular CSS imports); add `build.rollupOptions.output.manualChunks` in all three `vite.config.mts` to split vendor deps.                           |
| be5852 | P3       | RabbitMQ publish-channel race leaks connections (`apps/api/email-service/src/main.ts:38-57` and the email-subscription copy). Caches the channel, not a connect promise: two concurrent first requests each open a connection and the loser idles forever; error paths null the channel but never close the connection.                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Cache the promise; close on error.                                                                                                                                                                                                                                                     |
| be5cf7 | P3       | `common-node` barrel drags winston/archiver/qrcode into every service (`libs/@vigilant-broccoli/common-node/src/index.ts` re-exports the whole lib). Services importing only `getEnvironmentVariable` pay require-time + image weight for all three.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Split entry points.                                                                                                                                                                                                                                                                    |
| d47732 | P3       | `deploy-notify` npm-installs `socket.io-client` per notification (`.github/actions/deploy-notify/action.yml:33-35`). Now pinned + `--ignore-scripts` (#109) but still 2+ registry installs per deploy.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Replace the emit with a plain HTTPS POST or cache the install.                                                                                                                                                                                                                         |
| eef44b | P3       | OCI VMs pay full apt provisioning on every replacement. All three cloud-inits do `package_update/upgrade` + Docker install at first boot; fine while replacements are rare. Also ~700MB `google-cloud-cli` is baked into the GCP image just to read 4 secrets at first boot.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Bake an OCI image if replacements become routine; replace google-cloud-cli with a metadata-token + Secret Manager REST `curl`.                                                                                                                                                         |
| ef2df0 | P3       | hearth misc nits: new `S3Client` per R2 operation (`api/where-is/r2.ts:8-9`; a 10-image POST = 10 clients); chat calendar-intent path pays a full non-streamed completion before the streamed one (`api/chat/route.ts`); `home-provider.tsx` context value recreated per render; `where_is_items` GET has no pagination/column list; where-is create refetches the full list to learn the new item's id (`where-is/page.tsx`).                                                                                                                                                                                                                                                                                                                                             | Reuse one S3Client; return the created item from the POST; memoize the context value; add pagination/column list.                                                                                                                                                                      |
| f0a1b2 | P3       | vb-manager misc nits: `buildFileTree` awaits `stat` per entry serially (`api/docs/structure/route.ts:29`); weather route is `force-dynamic` with no server cache.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Parallelize the stats; add a server cache to the weather route.                                                                                                                                                                                                                        |
| f2e3a4 | P3       | Shared-component nits: `CRUDListManagement.tsx` recreates handlers and re-renders every row on any list change (memo a row component before lists grow); `ThemeProvider.tsx:57` context value recreated per render; `GithubActionsBadges.tsx` refetches the workflows API on every mount, uncached.                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Memoize a row component + context values; cache the badges fetch.                                                                                                                                                                                                                      |
| f4d5e6 | P3       | Shell/dotfile nits: `setup/dotfiles/zsh/scripts/docker_cleanup.sh:15` uses `stat -f %m` (breaks the 7-day throttle on Linux) and hits the Docker daemon in its check; `.rc.zsh:10` forks `sysctl\|grep` per shell, `:37` re-sources `~/.bash_profile`, `:39` re-sources tmux conf per shell; `load_aliases` spawns ~5 `find`s + ~20 `source`s per startup.                                                                                                                                                                                                                                                                                                                                                                                                                 | Portable mtime check; guard the daemon call; reduce per-shell forks and re-sources.                                                                                                                                                                                                    |
| f5e6f7 | P3       | Manual-op script nits: `backup-secrets.sh` makes several ~1s `bw` round trips per note chunk; `secret-rotation:all` chains five scripts each opening its own IAP tunnel (~5–10s each); `check-cloudflare-access-security.sh` re-fetches the Cloudflare IP list per hostname and spawns `python3` per CIDR; root `format` (`package.json:8`) runs prettier over the whole repo with no `--cache`; `scripts/shell/oci-ssh.sh:8-12` pays `terraform output` + `ssh-keyscan` before every SSH; nx `parallel` left at default 3.                                                                                                                                                                                                                                                | Batch `bw` lookups; share one IAP tunnel; cache the CF IP list; add prettier `--cache`; cache the OCI IP and keyscan only on host-key failure.                                                                                                                                         |

## Maintenance

| ID     | Priority | Description                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Recommended Fix                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ------ | -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| e17c40 | P2       | `vb-manager-next-mobile` is deployed to fly but monitored by nothing (`.upptimerc.yml` no `sites:` entry; `.github/workflows/ci-health-check.yml` no check). It has deploy targets, staging+production fly configs (both with a public `http_service` on `force_https`), a `manual-deploy-app.yml` entry, and secrets wiring (`scripts/secrets-mapping.config.ts:47`) — the only publicly-reachable deployed app with no status check in either system, which CLAUDE.md's CI.md rule forbids.                                                                                                                                                                                          | 1. Add `staging-vb-manager-next-mobile` and `production-vb-manager-next-mobile` to `sites:` in `.upptimerc.yml`, following existing fly entries' shape and staging-then-production ordering.<br>2. Pick a path returning 200 unauthenticated (sibling Supabase-auth apps use a login route, e.g. `staging-hearth` → `/login`; check whether the mobile root redirects first).<br>3. If the app is actually dormant, retire it instead (fly configs, deploy targets, manual-deploy option, secrets mapping).                                                                                                                                                                                                                      |
| 1f0a7e | P2       | Next.js "inferred workspace root" warning. Multiple Next.js apps (`small-business-next`, `vb-manager-next`, `vb-manager-next-mobile`, `whiteboard`, `findme`, `hearth`, `employee-handler-ui`) log "Next.js inferred your workspace root, but it may not be correct" — caused by two lockfiles (root `pnpm-lock.yaml` and `projects/nx-workspace/pnpm-lock.yaml`). Deferred — CI builds pass today (warning only).                                                                                                                                                                                                                                                                     | Set `outputFileTracingRoot` (or `turbopack.root`) explicitly in each app's Next.js config, or remove the redundant lockfile; revisit if a tracing-root-sensitive deploy issue surfaces, especially for hearth's Vercel serverless `sharp` bundling.                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 1c8bcf | P3       | Framework surface: the Angular 21 toolchain (~25 devDependencies) exists for one app, `cloud-8-skate-angular`, plus the `libs/angular/general-components` lib it consumes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Migrating it to the React/Next stack removes the largest maintenance burden in the workspace.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| 4d81ba | P3       | Resume markup is implemented twice and has already drifted once (`libs/@vigilant-broccoli/resume/src/server.ts` `buildResumeHtml`/`renderWorkExperience` for the Playwright PDF vs `apps/ui/vb-manager-next/src/app/components/resume-view.component.tsx` JSX for `/career`). #222 made `resume.json` the single source of data, but presentation is written twice — #255 added Project Experience to the React view only and `vigilant-broccoli` dropped out of the published `resume.pdf` until #257 patched `server.ts` a day later. The PDF is the artifact sent to people and has no UI to notice drift.                                                                          | Render one tree: render the React view server-side (`react-dom/server` `renderToStaticMarkup`) inside `generateResumePdfBuffer` and drop `buildResumeHtml`, keeping the print stylesheet as the only PDF-specific piece; or move the shared section/entry components into the `resume` lib so both consumers import them. Acceptance: add a Project Experience entry to `resume.json` and confirm it appears in both `/career` and a regenerated `resume.pdf` without touching two files.                                                                                                                                                                                                                                        |
| ce18a7 | P3       | No shared `localStorage` state hook — 9 hand-rolled copies split across two incompatible strategies. `react-lib/src/hooks/` has only `useGeolocation.ts`, so every UI-preference persistence site re-implements read→validate→write by hand: Strategy A (read in `useEffect`, SSR-safe but flashes the default) in ThemeProvider, CollapsibleList, quick-links, search-dialog, LanguageLearning, dev-dashboard; Strategy B (lazy `useState` behind `typeof window`, no flash but hydration mismatch) in kanban, google-tasks (×2), useNotepad. Also `search-dialog` and `quick-links` share `LOCAL_STORAGE_KEY = 'quick-links-grouped-state'`, one key backing two independent states. | Add one `useLocalStorageState<T>(key, default, { isValid?, parse?, serialize? })` in `react-lib/src/hooks/` returning `{ value, setValue, hydrated }`, reading in `useEffect` (lift ThemeProvider's `storage`-event listener), exported from the barrel (public API — keep side-effect-free). Migrate strategy-A then strategy-B sites; fold in the JSON stores (useChatHistory/useNotificationHistory/useNotepad) only if parse/serialize land; fix the `'quick-links-grouped-state'` collision; preserve every existing key string verbatim; out of scope: auth-token storage (see ae83d3). Verify with `npx nx lint react-lib vb-manager-next` + typecheck + manual reload; release react-lib per its `publish-package` flow. |

## Feature Enhancements

| ID     | Priority | Description                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Recommended Fix                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| ------ | -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| f7afb2 | NA       | Provision Vault JWT policies/roles declaratively instead of the imperative post-init script (`infrastructure/terraform/scripts/post-apply.sh`, `packer/scripts/run-vault-post-init.sh`, `config.sh`, `package.json` `gcp:vm:post-init`). Vault's JWT auth config (the `github-actions-role`/`-rotate-role`/`-pr-check-role` roles + policies) is written by the imperative `run-vault-post-init.sh`, really a VM-bootstrap script. #217 made `post-apply.sh` call `npm run gcp:vm:post-init` on the IP-unchanged path so a new role isn't silently skipped — but that re-runs the whole heavy bootstrap over IAP SSH on every apply, with policy/role definitions living as here-doc strings. | Manage policies/roles as first-class Terraform resources (`hashicorp/vault` provider's `vault_policy` + `vault_jwt_auth_backend_role`): 1. Add the provider to `main.tf` reaching Vault over WireGuard `10.0.1.1:8200`, sourcing the root token the way `load-vault-tf-env.sh` does.<br>2. Port the two policies + three roles keeping exact names/paths/TTLs/`bound_claims`.<br>3. `terraform import` the live policies/roles so the first apply is a no-op.<br>4. Strip the policy/role/`kv/test` blocks from `run-vault-post-init.sh` (leave init + mount enable); remove the interim `post-init` call from `post-apply.sh`.<br>5. Update `secret-management.md` and any `pnpm gcp:vm:post-init` reference.<br>6. Verify `tf:plan` no-op, then edit a `bound_claims` and confirm plan/apply reconcile without touching the VM; confirm `ci-pr-check`/`ci-rotate-secrets` still authenticate to Vault.                                                                                                                                                                                                                                                                |
| b848da | NA       | Self-host journal.harryliu.dev on gitea-vm instead of Cloudflare Pages, so notes stop resting on third-party storage (`cloudflare-journal.tf`, `.github/workflows/cron-deploy-journal.yml`, `journal/project.json`). `journal.harryliu.dev` is gated by CF Zero Trust Access, but the notes content still rests in plaintext on a GitHub-hosted runner (hourly `cron-deploy-journal.yml` fetches the private notes archive from Gitea, builds `build-snapshot.mjs`, deploys to `staging-journal` Pages) and on Cloudflare Pages storage.                                                                                                                                                      | Serve journal directly from the self-hosted `gitea-vm` (same trust model as git/code.harryliu.dev): 1. Add a `journal` service to `cloud-init-gitea.yaml`'s compose reading notes from the bare repo under `/mnt/gitea-data` (`git --git-dir=... archive main`, no network fetch/token), refreshing on a short interval; add a `Caddyfile` site block.<br>2. Add `cloudflare_origin_ca_certificate` + `tls_cert_request` for `var.journal_domain` copying `cloudflare-gitea.tf:26-31`.<br>3. In `cloudflare-journal.tf` replace `cloudflare_pages_domain` with a proxied A `cloudflare_dns_record` to `oci_core_instance.gitea.public_ip`; keep the Access app/policies, drop `*.pages.dev` destinations.<br>4. Delete `cron-deploy-journal.yml` and the `deploy`/`ensure-cf-project`/`prune-deployments`/`manual-deploy` targets in `project.json`; decide whether the Vite `build` still runs in CI for lint/typecheck.<br>5. Retire the `staging-journal` Pages project.<br>6. Update `network-management.md`'s journal line and revisit `repo-patterns.md`'s single-environment note.<br>7. Also removes the recurring `cron-deploy-journal` rebuild cost entirely. |
| a3f9c2 | NA       | hearth where-is: search only matches the AI's literal wording, no semantic fallback (`apps/hearth/src/app/where-is/page.tsx:38-45` `fuzzyMatch`; `api/where-is/analyze/route.ts` LLM pipeline). `fuzzyMatch` requires every query word to appear as a literal substring in the concatenated title/description/tags (all AI-generated) — searching "scissors" against a drawer tagged "cutting tools" returns nothing, undercutting the feature exactly when the AI's wording differs from the user's.                                                                                                                                                                                         | 1. Add `api/where-is/search/route.ts`: accept `{ query, homeId }`, fetch that home's items (title/description/tags only), prompt the LLM to return best-matching item id(s) + a short "why", falling back to "no match".<br>2. In `where-is/page.tsx` call it when `fuzzyMatch` returns zero for a non-empty query, surfacing matched item(s) + "why".<br>3. Keep the literal `fuzzyMatch` as the fast first pass — only pay for the LLM call on a miss.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| d7e1b4 | NA       | hearth where-is: flat storage-area list has no room/zone grouping (`where-is/page.tsx` flat `CRUDItemList`; `where-is-form.tsx` freeform `tags`; `lib/types.ts` `WhereIsItem`). Storage areas are a flat, freeform-titled list with tags but no structured location field — no way to browse "just the kitchen". Browsing stops working past a dozen entries.                                                                                                                                                                                                                                                                                                                                 | 1. Cheapest: treat it as a convention over the existing `tags` array (e.g. a `room:kitchen` tag) — no migration, the tag UI already supports free entry.<br>2. If too implicit, add a nullable `room` text column to `where_is_items` plus a small fixed suggestion list.<br>3. In `where-is/page.tsx` derive the distinct rooms from loaded items and render filter chips above the search input, combinable with the text search (AND).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |

## UI Cleanup

Deprecating `@radix-ui/themes` component-by-component (see the shim precedent in `libs/@vigilant-broccoli/react-lib/src/components/Text.tsx` / `Heading.tsx` — cva + Tailwind + `cn`, `asChild` via `@radix-ui/react-slot`). `Text`, `Heading`, and `Button` are already migrated. One row per remaining component; counts are import sites; ordered easy → hard. `Theme` must go last (it also carries `@radix-ui/themes/styles.css`).

| ID     | Priority | Description                                                                                                                             | Recommended Fix                                                                                                                                                                            |
| ------ | -------- | --------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 59b668 | P1       | Difficulty **Easy**. `Flex` (3 sites) — layout-only wrapper, no react-lib equivalent.                                                   | Replace with a `<div>` using Tailwind flex utilities; map `gap`/`direction`/`align`/`justify` props to classes. No shim needed.                                                            |
| b22ca1 | P1       | Difficulty **Easy**. `Code` (3) — inline code styling.                                                                                  | Replace with `<code>` + a mono/bg utility class, or a tiny `Code` shim mirroring `Text.tsx`.                                                                                               |
| d6925a | P1       | Difficulty **Easy**. `Link` (2).                                                                                                        | Replace with `<a>` + underline/color utilities, or a small `Link` shim.                                                                                                                    |
| e9990b | P1       | Difficulty **Easy**. `VisuallyHidden` (5) — a11y-only.                                                                                  | Add a `VisuallyHidden` helper in react-lib rendering `<span className="sr-only">`; repoint imports.                                                                                        |
| b61d0f | P1       | Difficulty **Easy**. `Spinner` (2).                                                                                                     | Replace with lucide `Loader2` + `animate-spin` (the pattern already in `react-lib/src/components/Button.tsx`).                                                                             |
| ba8713 | P1       | Difficulty **Easy** (high volume, 42 sites). `Badge` — styled label span with `color`/`size` props.                                     | Add a cva-based `Badge` in react-lib mirroring `Text.tsx`/`Heading.tsx` (color→bg/text utilities, size→padding/text), then codemod imports off `@radix-ui/themes`.                         |
| 441aed | P1       | Difficulty **Easy**. `Switch` (2) — react-lib already exports `Switch` (`components/Switch.tsx`).                                       | Repoint imports to `@vigilant-broccoli/react-lib`; map `checked`/`onCheckedChange`.                                                                                                        |
| 46ca81 | P1       | Difficulty **Easy**. `ScrollArea` (5) — react-lib already exports `ScrollArea`.                                                         | Repoint imports; verify scrollbar-styling parity.                                                                                                                                          |
| f8d87d | P1       | Difficulty **Easy**. `TextArea` (1) — react-lib exports `Textarea`.                                                                     | Repoint the single import and map props.                                                                                                                                                   |
| f9dd21 | P2       | Difficulty **Medium** (17). `Card` — react-lib has `CardContainer`.                                                                     | Repoint to `CardContainer`, or add a styled `Card` shim (border/rounded/padding); check the few `Card` variant usages first.                                                               |
| b883ce | P2       | Difficulty **Medium** (6). `Callout` — compound `Callout.Root/Icon/Text`.                                                               | Add a `Callout` component in react-lib (styled div with icon slot + color variants); repoint.                                                                                              |
| 8c08fa | P2       | Difficulty **Medium** (8). `TextField` — compound `TextField.Root/Slot`; react-lib has `Input`.                                         | Repoint to react-lib `Input`, converting `TextField.Root` + `Slot` to `Input` + adornment.                                                                                                 |
| 22c928 | P2       | Difficulty **Medium** (5). `Table` — compound `Table.Root/Header/Body/Row/Cell/ColumnHeaderCell`.                                       | Replace with a native `<table>` + Tailwind, or a small `Table` shim exposing the same subcomponents.                                                                                       |
| e28da8 | P2       | Difficulty **Medium** (4). `Tabs` — react-lib exports `Tabs`.                                                                           | Repoint and map `Tabs.Root/List/Trigger/Content` to the react-lib API.                                                                                                                     |
| f07a1a | P2       | Difficulty **Medium** (2). `Select` — react-lib exports `Select`.                                                                       | Repoint and remap the compound `Select.Root/Trigger/Content/Item` usage.                                                                                                                   |
| 49207d | P2       | Difficulty **Medium** (1). `Slider`.                                                                                                    | Replace with a styled `<input type="range">`, or wrap the `@radix-ui/react-slider` primitive.                                                                                              |
| e397eb | P2       | Difficulty **Medium** (1). `SegmentedControl` (`Root/Item`).                                                                            | Build a toggle-group of buttons with an active class, or wrap `@radix-ui/react-toggle-group`.                                                                                              |
| 545ae1 | P2       | Difficulty **Medium** (1). `TabNav` — nav links with active state.                                                                      | Replace with a `<nav>` of links that styles the active route.                                                                                                                              |
| 7f2f0e | P3       | Difficulty **Hard** (25, highest volume). `Dialog` — focus trap + portal; react-lib already exports `Dialog` (`components/Dialog.tsx`). | Repoint to react-lib `Dialog` and remap the compound `Dialog.Root/Trigger/Content/Title/Description/Close` API; migrate in batches given the volume.                                       |
| be4521 | P3       | Difficulty **Hard** (6). `DropdownMenu` — keyboard nav + portal, no react-lib equivalent.                                               | Wrap `@radix-ui/react-dropdown-menu` (headless) in a styled react-lib component; repoint.                                                                                                  |
| 5a7e0f | P3       | Difficulty **Hard** (2). `AlertDialog` — confirm pattern.                                                                               | Add an `AlertDialog`/confirm on top of react-lib `Dialog`, or wrap `@radix-ui/react-alert-dialog`.                                                                                         |
| c68aa3 | P3       | Difficulty **Hard** (1). `Tooltip` — positioning.                                                                                       | Wrap `@radix-ui/react-tooltip` in a styled react-lib `Tooltip`.                                                                                                                            |
| a6ffe1 | P3       | Difficulty **Hard / blocked** (6). `Theme` — the `<Theme>` provider root; removing it also drops `@radix-ui/themes/styles.css`.         | Do **last**, only once every component above is migrated: replace with react-lib `ThemeProvider` + a minimal CSS reset. Also unblocks the Radix-CSS bundle-size cleanup tracked in 5bdea5. |

## Not so serious

Low-stakes items: deliberately-accepted risks from the audit (kept for visibility because this is a solo-admin, personal-scale deployment — revisit if the app gains other users or is exposed beyond loopback) and event-calendar nice-to-haves.

| ID     | Priority | Description                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Recommended Fix                                                                                                                                                                                                                                                                                                                                                                                                                      |
| ------ | -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| ff4327 | NA       | [security · non-risk] Seafile SSH open to 0.0.0.0/0, no brute-force mitigation (`infrastructure/terraform/aws-seafile.tf:20-25`). The SG's own `description` documents this as intentional ("SSH open, 80/443 restricted to Cloudflare"), mitigated by key-only auth (Ubuntu disables SSH password auth by default). No `fail2ban`/rate limiting, so the box is a continuous scan/brute-force target that fails without a key but consumes log volume. Acceptable for a single-admin personal server (compare 20177f). | If ever prioritized: restrict the port-22 `cidr_blocks` to a home/WireGuard egress IP, or add `fail2ban` via cloud-init.                                                                                                                                                                                                                                                                                                             |
| 16815c | NA       | [security · non-risk] `vb-manager-next`: Supabase Google sign-in has no allowlist — accepted risk, not fixed (`src/middleware.ts`, `libs/server-auth.ts`). `middleware.ts` verifies the Supabase bearer token via `supabase.auth.getUser(token)` and admits any valid user — any Google account that completes the OAuth flow passes the `/api/*` gate. Deliberately left open: owner's threat model is solo use, UI-only, app bound to `127.0.0.1`, no external callers.                                              | Revisit if ever exposed beyond loopback or a second Google account is expected: reject unless `data.user.email` is in an `ALLOWED_EMAILS`/`ALLOWED_EMAIL_DOMAINS` allowlist in `middleware.ts` + `getUserEmail`, mirroring `employee-handler-ui/src/proxy.ts`.                                                                                                                                                                       |
| b8169a | NA       | [security · non-risk] Default credentials in local docker-compose (`infrastructure/local/docker-compose.yml:82,140-141`): Grafana `admin`/`admin`, Immich Postgres `postgres`/`postgres`. Localhost-bound (mitigated).                                                                                                                                                                                                                                                                                                 | Set real passwords via the gitignored `.env`.                                                                                                                                                                                                                                                                                                                                                                                        |
| d19470 | NA       | [performance · non-risk] vb-express auth DB is synchronous `node:sqlite` (`apps/api/vb-express/src/auth.ts:3,31` `DatabaseSync`). Every session/API-key check blocks the event loop, serializing streaming responses under burst. Fine at personal scale; worth knowing.                                                                                                                                                                                                                                               | Move to an async driver if burst concurrency ever matters.                                                                                                                                                                                                                                                                                                                                                                           |
| e5a1c0 | NA       | Scheduled auto-sync for event calendars (`src/lib/event-scraper/sync-runner.ts`, `src/app/api/event-calendars/[id]/sync/route.ts`, `src/app/components/event-calendars.component.tsx`). Syncs only fire on calendar creation or a manual "Sync now", so a calendar silently goes stale between clicks.                                                                                                                                                                                                                 | Add a recurring sync (per-calendar interval stored on the row, or one nightly job for all tracked calendars). Each run drives real Chrome for minutes, so add a concurrency guard (reuse the runner's `runningSyncs` in-flight tracking to skip a calendar already syncing) and stagger launches. A node-cron scheduler runs only while PM2 is up; an external `pnpm` cron survives restarts but needs its own Vault/session access. |
| 3b7f92 | NA       | Adopt an untracked calendar into the table (`src/app/components/event-calendars.component.tsx`, `src/app/api/event-calendars/untracked/route.ts`, `src/lib/event-calendars.db.ts`). The untracked section only offers Delete.                                                                                                                                                                                                                                                                                          | Add an "Import" action that inserts an existing service-account-owned calendar into `event_calendars` (it already holds the right events) instead of forcing a delete + re-scrape. Needs a name and ideally the source URL(s) to attach (without sources the row can't be re-synced). Mostly worth doing only if orphans actually recur.                                                                                             |
