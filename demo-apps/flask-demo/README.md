# Flask Demo

## Commands

macOS / Linux Setup

```
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

Windows Setup

```
python -m venv venv
source venv/Scripts/activate
pip install -r requirements.txt
```

```
python run.py
```
