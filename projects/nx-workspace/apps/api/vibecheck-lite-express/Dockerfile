# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build vibecheck-lite-express`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t vibecheck-lite-express`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system vibecheck-lite-express && \
          adduser --system -G vibecheck-lite-express vibecheck-lite-express

COPY dist/apps/api/vibecheck-lite-express vibecheck-lite-express
RUN chown -R vibecheck-lite-express:vibecheck-lite-express .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix vibecheck-lite-express --omit=dev -f install

CMD [ "node", "vibecheck-lite-express" ]
