# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build app-monitor-api`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t app-monitor-api`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system app-monitor-api && \
    adduser --system -G app-monitor-api app-monitor-api

COPY dist/apps/api/app-monitor-api app-monitor-api
RUN chown -R app-monitor-api:app-monitor-api .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix app-monitor-api --omit=dev -f install

CMD [ "node", "app-monitor-api" ]
