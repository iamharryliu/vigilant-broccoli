import path from 'path';

export const CACHE_FOLDER = path.resolve(process.cwd(), '.cache');
export const CONFIG_FOLDER = path.resolve(process.cwd(), 'config');
export const TMP_PATH = path.resolve(process.cwd(), 'tmp');
export const QA_FOLDER = path.resolve(process.cwd(), 'qa');
export const SECRETS_FOLDER = path.resolve(process.cwd(), '.secrets');
export const LOGS_FOLDER = path.resolve(process.cwd(), 'logs');
