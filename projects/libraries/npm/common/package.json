{
  "name": "@prettydamntired/test",
  "version": "0.0.7",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "jest --coverage .",
    "format:commit": "prettier --write",
    "test:commit": "jest --bail --findRelatedTests",
    "prepublish": "npm test",
    "buildnpublish": "npm version patch && npm run build && npm publish --access=public"
  },
  "devDependencies": {
    "@types/jest": "^29.5.11",
    "jest": "^29.7.0",
    "mongoose": "^8.1.0",
    "ts-jest": "^29.1.2",
    "winston": "^3.11.0"
  },
  "dependencies": {
    "rxjs": "^7.8.1"
  }
}
