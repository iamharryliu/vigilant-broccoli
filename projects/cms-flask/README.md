# CMS System Flask

## Commands

```
# Setup venv MacOS
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt

# Setup venv Windows
python -m venv venv
source venv/Scripts/activate
pip install -r requirements.txt

python run.py
```
